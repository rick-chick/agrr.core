# Core dependencies
requests>=2.25.0
pandas>=1.3.0
numpy>=1.20.0
matplotlib>=3.5.0

# Time series prediction dependencies
statsmodels>=0.13.0

# Optional dependencies (install separately if needed)
# prophet>=1.1.0  # Commented out due to Windows path length issues

# LLM (optional)
openai>=1.50.0
python-dotenv>=1.0.0

